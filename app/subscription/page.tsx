export const dynamic = "force-dynamic";
import { PricingTable } from "@clerk/nextjs";

const Page = () => {
    return (
        <main>
            <PricingTable />
        </main>
    )
}

export default Page;